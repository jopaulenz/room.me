<!DOCTYPE html>
<html>
  <head>
    <link href="https://fonts.googleapis.com/css2?family=Fredoka:wght@300..700&display=swap" rel="stylesheet">
    <title>RoomMe</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
  <div class="page_container">
    <div class="content-container"><%= yield %>
    </div>
    <div class="navbar-container"><%= render "shared/navbar_home" %>
    </div>
  </div>
    <%# Render specific navbar only on relevant pages %>
    <%# <% unless controller_name == "living_preferences" && (action_name == "new" || action_name == "edit") %>
    <%# end %>

    <%= render "shared/flashes" %>
  </body>
</html>
